## 1. Лимит notice

- [x] 1.1 Вычислять лимит результата команды как `min(12, max(3, height/3))` и передавать его в `clipText(m.status, ...)`; проверить тестом на низком и обычном терминале
- [x] 1.2 Убедиться тестом, что при многострочном результате viewport остаётся не меньше одной строки, а input отображается

## 2. Проверка

- [x] 2.1 Прогнать `go build ./...` и `go test ./week-02/...` — оба зелёные
- [x] 2.2 Вручную: `/stats` после нескольких стратегий показывает больше одной записи в кадре
